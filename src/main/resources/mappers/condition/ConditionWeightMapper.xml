<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kbait.anchack.condition.mapper.ConditionWeightMapper">

  <insert id="insertBatch">
    INSERT INTO condition_weights (condition_id, category, importance)
    VALUES
    <foreach collection="rows" item="row" separator=",">
      (#{row.conditionId}, #{row.category}, #{row.importance})
    </foreach>
  </insert>

  <select id="findByConditionId" resultType="com.kbait.anchack.condition.dto.ConditionWeightRow">
    SELECT
      condition_id AS conditionId,
      category,
      importance
    FROM condition_weights
    WHERE condition_id = #{conditionId}
  </select>

</mapper>
