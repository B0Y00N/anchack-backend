<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kbait.anchack.favorite.mapper.FavoriteDongMapper">

  <select id="findByUserId" resultType="com.kbait.anchack.favorite.domain.FavoriteDong">
    SELECT
      admin_dong_id AS adminDongId,
      created_at AS createdAt
    FROM favorite_dongs
    WHERE user_id = #{userId}
    ORDER BY created_at DESC
  </select>

  <insert id="insertIgnore">
    INSERT IGNORE INTO favorite_dongs (user_id, admin_dong_id)
    VALUES (#{userId}, #{adminDongId})
  </insert>

  <delete id="delete">
    DELETE FROM favorite_dongs
    WHERE user_id = #{userId}
      AND admin_dong_id = #{adminDongId}
  </delete>

</mapper>
